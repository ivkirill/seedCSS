/core/ - основные less файлы составл€ющие €дро Seed CSS Frame
/core/mixins/ - общие примеси проекта
/libs/ - готовые stand-alone сборки дл€ отдельных библиотек

seed.less - основной файл Seed CSS Frame, содержит все иморты
seed.config.less - конфигурационный файл, описывает переменные

ƒл€ использовани€ Seed CSS Frame в новом проекте, необходимо:

1. скопировать файлы
- seed.less
- seed.config.less
в папку "/i/css/" проекта

2. переименовать согласно директиве нейминга:
- {ProjectName}.seed.less
- {ProjectName}.seed.config.less
где {ProjectName} - это им€ проекта

3. настроить {ProjectName}.seed.config.less файл, указать все переменные по дизайну
4. подключить все остальные CSS\LESS файлы проекта в основной файл {ProjectName}.seed.less
5. подключить основной файл {ProjectName}.seed.less в HEAD проекта.

ƒл€ использовани€ LESS файлов отдельных библиотек:

1. подключить нужный файл библиотеки из папки /libs/ в HEAD проекта, либо через import в основном файле
2. переопределить необходимые по дизайну класса и правила.
